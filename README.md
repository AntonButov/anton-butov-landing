This is a Kotlin Multiplatform project targeting Web.

run
```agsl
./gradlew wasmJsBrowserDevelopmentRun
```

## Optimization

Unused fonts were removed to reduce the bundle size, which speeds up the first launch of the web app.

